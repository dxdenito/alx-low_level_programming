#include <stdio.h>

/**
 * main - print the name of file
 * @argc: argument count
 * @argv: array of strings
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
